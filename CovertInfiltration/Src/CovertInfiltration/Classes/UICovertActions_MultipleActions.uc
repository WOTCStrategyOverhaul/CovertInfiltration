class UICovertActions_MultipleActions extends UICovertActions;

// This class serves no functional purpose
// Especially because UICovertActions should no longer be ever used
// So we have this MCO to have an obvious conflict if another mod overrides UICovertActions
// Since the other mod will be guranteed gameplay incompatible